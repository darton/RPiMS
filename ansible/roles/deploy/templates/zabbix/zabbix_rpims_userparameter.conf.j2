#Uncomment the lines with the sensors you are using

#For current modern version of RPiMS templates for Zabbix
UserParameter=rpims.sensors.json,curl -s http://127.0.0.1/api/data/sensors/all

#####For old version of RPiMS templates for Zabbix
#UserParameter=rpims.cputemp[*],curl -s http://127.0.0.1/api/data/sensors/cpu |jq -r '.temperature'
#For BME280 sensor simple verion
#UserParameter=rpims.id3_bme280[*],sh -c 'curl -s http://127.0.0.1/api/data/sensors/bme280 | jq -r --arg idx "$1" ".id3 | { \"1\": \"Temperature\", \"2\": .temperature, \"3\": \"Humidity\", \"4\": .humidity, \"5\": \"Pressure\", \"6\": .pressure }[\$idx]"' _ $1
